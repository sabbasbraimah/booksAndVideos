spring:
  datasource:
    url: jdbc:mysql://localhost:3306/booksAndVideos_dev
    username: kofi
    password: sabbas
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
    properties:
     javax:
        persistence:
         schema-generation:
           create-source: metadata
            scripts:
              action: create
              create-target: booksAndVideos.sql